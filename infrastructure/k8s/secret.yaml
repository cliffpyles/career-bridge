# NOTE: This is a template. Do NOT commit real secrets here.
# In production, populate secrets via:
#   kubectl create secret generic career-bridge-secrets \
#     --from-literal=DATABASE_URL="..." \
#     --from-literal=SECRET_KEY="..." \
#     --from-literal=OPENAI_API_KEY="..." \
#     --namespace career-bridge
#
# Or use Google Secret Manager with Workload Identity.
apiVersion: v1
kind: Secret
metadata:
  name: career-bridge-secrets
  namespace: career-bridge
  labels:
    app.kubernetes.io/name: career-bridge
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://postgres:REPLACE_ME@cloud-sql-proxy:5432/career_bridge"
  SECRET_KEY: "REPLACE_ME_WITH_STRONG_RANDOM_KEY"
  OPENAI_API_KEY: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
